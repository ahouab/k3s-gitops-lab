# Minimal values for Argo CD Helm installation (example)

server:
  ingress:
    enabled: true
    hosts:
      - argocd.lab.local
    paths:
      - /
    tls:
      - secretName: argocd-tls
        hosts:
          - argocd.lab.local
